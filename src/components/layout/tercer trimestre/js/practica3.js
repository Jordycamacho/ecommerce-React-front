function mostrarMensaje() {
    alert("Hola, soy un script externo");
  }
  
  // Llamamos a la función para que muestre el mensaje
  mostrarMensaje();
  
  // Esta es otra función que muestra un mensaje en la consola del navegador
  function mostrarOtroMensaje() {
    alert("Soy el primer script");
  }
  
  // Llamamos a la función para que muestre el otro mensaje
  mostrarOtroMensaje();